# Core audio
sounddevice>=0.4.7
numpy<2.0.0
webrtcvad-wheels>=2.0.10

# STT — Apple Silicon (mlx-whisper), fallback on other platforms
mlx-whisper>=0.4.0; sys_platform == "darwin" and platform_machine == "arm64"
faster-whisper>=1.0.3; not (sys_platform == "darwin" and platform_machine == "arm64")

# TTS
piper-tts>=1.3.0

# LLM clients — Claude Code CLI + Codex CLI called as subprocesses (no API keys)
# Ollama (offline) reached over HTTP via requests
requests>=2.32.0

# Tools — web scraping + screen OCR
beautifulsoup4>=4.12.0
Pillow>=10.4.0
pytesseract>=0.3.13

# Dictation — native macOS Quartz event tap (works on macOS 26 Tahoe; pynput does not)
pyobjc-framework-Quartz>=10.0; sys_platform == "darwin"
pyperclip>=1.9.0

# GUI
PyQt6>=6.6.0
